% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/skill_remove.R
\name{skill_remove}
\alias{skill_remove}
\title{Remove installed skills}
\usage{
skill_remove(
  skill,
  path = ".",
  force = FALSE,
  target = names(grimoire_targets)
)
}
\arguments{
\item{skill}{Character vector of skill names to remove.}

\item{path}{Base directory skills were installed into with \code{\link[=use_skill]{use_skill()}}.}

\item{force}{If \code{FALSE} (the default), a locally modified skill is
skipped with a warning. If \code{TRUE}, it is removed anyway.}

\item{target}{Where under \code{path} the skills were installed; must match
the \code{target} used with \code{\link[=use_skill]{use_skill()}}. See \code{\link[=use_skill]{use_skill()}} for the
available values.}
}
\value{
The names of the skills that were removed, invisibly.
}
\description{
Deletes previously installed skill folders. A skill that has been
hand-edited locally (its on-disk checksum no longer matches what
\code{\link[=use_skill]{use_skill()}} installed) is left in place unless \code{force = TRUE}. When the
last skill at \code{path} is removed, the manifest file (and the now-empty
\verb{skills/} directory) are cleaned up too.
}
\examples{
\dontrun{
skill_remove("rladies-blog-post")
}

}
