% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/use_skill.R
\name{use_skill}
\alias{use_skill}
\title{Fetch a skill into a local project}
\usage{
use_skill(
  skill,
  path = ".",
  repo = "rladies/grimoire",
  ref = NULL,
  force = FALSE,
  target = names(grimoire_targets)
)
}
\arguments{
\item{skill}{Character vector of skill names, as listed by
\code{\link[=available_skills]{available_skills()}}.}

\item{path}{Base directory to install into. Skills land in
\verb{<path>/skills/<skill>/}. Defaults to the current directory.}

\item{repo}{\code{"owner/repo"} slug of the source repository.}

\item{ref}{Git ref (tag, branch, or SHA) to install from. Defaults to the
latest 'GitHub' release, falling back to the default branch if the repo
has no releases.}

\item{force}{If \code{FALSE} (the default), an already-installed skill is left
as-is. If \code{TRUE}, it is deleted and re-downloaded, discarding any local
edits.}

\item{target}{Where under \code{path} to install, relative to the skill/tool
that should auto-discover it: \code{"default"} installs to \verb{skills/},
\code{"claude"} to \verb{.claude/skills/} (read by Claude Code and Opencode),
\code{"opencode"} to \verb{.opencode/skills/}, and \code{"agents"} to \verb{.agents/skills/}.}
}
\value{
The path(s) to the installed skill folder(s), invisibly.
}
\description{
Downloads one or more skills from a grimoire-format 'GitHub' repository
into \verb{<path>/skills/<skill>/}, so any tool that reads the Agent Skills
format (Claude Code, 'opencode', or others) can use it locally. Already
installed skills are left untouched unless \code{force = TRUE}.
}
\examples{
\dontrun{
use_skill("rladies-blog-post")
use_skill(c("rladies-voice", "rladies-brand"), path = "my-project")
use_skill("rladies-blog-post", target = "opencode")
}

}
