% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/skill_update.R
\name{skill_update}
\alias{skill_update}
\title{Update installed skills to a newer ref}
\usage{
skill_update(
  skill = NULL,
  path = ".",
  ref = NULL,
  force = FALSE,
  target = names(grimoire_targets)
)
}
\arguments{
\item{skill}{Character vector of skill names to update, or \code{NULL} (the
default) to update every skill installed at \code{path}.}

\item{path}{Base directory skills were installed into with \code{\link[=use_skill]{use_skill()}}.}

\item{ref}{Git ref to update to. Defaults to the latest 'GitHub' release
of the repository the skills were originally installed from.}

\item{force}{If \code{FALSE} (the default), a locally modified skill is
skipped with a warning. If \code{TRUE}, it is overwritten.}

\item{target}{Where under \code{path} the skills were installed; must match
the \code{target} used with \code{\link[=use_skill]{use_skill()}}. See \code{\link[=use_skill]{use_skill()}} for the
available values.}
}
\value{
The names of the skills that were updated, invisibly.
}
\description{
Re-downloads installed skills whose upstream content has moved on. A
skill is skipped if it has been hand-edited locally (its on-disk checksum
no longer matches what \code{\link[=use_skill]{use_skill()}} installed), unless \code{force = TRUE}.
}
\examples{
\dontrun{
skill_update() # update everything installed
skill_update("rladies-blog-post")
}

}
